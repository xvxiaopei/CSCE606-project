<tr>
                <td><%= user.id %></td>
                <td><%= user.email %></td>
                <td><%= user.name %></td>
                <% if Fullsubmission.find_by_user_id(user.id)!=nil %>
                <td><%= Fullsubmission.find_by_user_id(user.id).count %></td>
                <td><%= Fullsubmission.find_by_user_id(user.id).num_correct %></td>
                <td><%= Fullubmission.find_by_user_id(user.id).accuracy.round(3) %></td>
                <% else %>
                <td>0</td>
                <td>0</td>
                <td>0</td>
                <% end %>
</tr>
